apiVersion: v1
kind: Service
metadata:
  namespace: ${k8s-namespace}
  name: ${k8s-backend-service-name}
spec:
  selector:
    app: ${k8s-backend-app-name}
  ports:
  - port: 8080
    targetPort: 8080